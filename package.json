{
  "name": "github-actions-release-test",
  "private": true,
  "version": "1.0.9",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./lib && tsc",
    "check": "node scripts/check_and_push_tag.js",
    "deploy": "gh-pages -d public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@octokit/rest": "^18.0.4",
    "@types/node": "^14.6.0",
    "gh-pages": "^3.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}
