{
  "name": "github-actions-release-test",
  "private": true,
  "version": "1.0.7",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./lib && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@octokit/rest": "^18.0.4",
    "@types/node": "^14.6.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}
